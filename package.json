{
  "name": "bob-workspace",
  "version": "1.4.0",
  "description": "BOB Workspace — fully configurable GUI workspace layer for Obsidian: custom dashboards, visual pipelines, schemas, project management, and daily planning on plain markdown.",
  "main": "main.js",
  "private": true,
  "scripts": {
    "build": "node esbuild.config.mjs production",
    "dev": "node esbuild.config.mjs",
    "typecheck": "tsc --noEmit",
    "test": "npm run build && node tests/run-tests.js",
    "check": "npm run typecheck && npm run build && node --check main.js && node tests/run-tests.js",
    "sync-schemas": "node scripts/sync-template-schemas.mjs"
  },
  "keywords": [
    "obsidian",
    "obsidian-plugin"
  ],
  "author": "cbruyndoncx",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.10.0",
    "esbuild": "^0.24.0",
    "obsidian": "^1.7.2",
    "typescript": "^5.7.2"
  }
}
